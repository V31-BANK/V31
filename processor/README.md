### Annotation Processor JAR

Runs during compilation to generate Java source code or metadata.

- Used only at compile time.
- Should be declared through the `annotationProcessor` dependency configuration.
- Does not provide runtime application functionality.

