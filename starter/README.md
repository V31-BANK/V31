### Starter JAR

Aggregates the dependencies required to use a feature.

- Contains no implementation code.
- Allows a service to enable a feature by adding a single dependency.
- Typically depends on the corresponding auto-configuration JAR and its default libraries.

